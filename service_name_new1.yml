---
- hosts: all
  remote_user: root
  become: true
  vars:
    service_name: "{ kdump.service }"
  
  tasks:
      - name: ensure service state running ---
        service:
             name: kdump.service
             state: started
